@model IEnumerable<WebApp.DTO_Models.Final.ExaminationControllerView>

    @{
    ViewData["Title"] = "Index";
}

<h1>Welcome</h1>


<table class="table table-hover width=80% table-light" style = "text-align: center">
    <thead class="table-primary">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ExamCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Candidate.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExamDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResultIssueDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResultLabel)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
                <tr>
                    <td>
                    @Html.Raw(item.ExamCode)
                    </td>
                    <td>
                    @Html.Raw(item.Candidate.LastName)
                    </td>
                    <td>
                    @Html.Raw(item.ExamDate)
                    </td>
                    <td>
                    @Html.Raw(item.ResultIssueDate)
                    </td>
                    <td>
                    @Html.Raw(item.ResultLabel)
                    </td>
                    @*<td >
                        <a class="btn btn-link" asp-action="Edit" asp-route-id="@item.QuestionId">Edit Display</a> |
                        <a class="btn btn-link" asp-action="EditAnswersIndex" asp-route-id="@item.QuestionId">Edit Answers</a> |
                        <a class="btn btn-link" asp-action="EditQuestionTopicsIndex" asp-route-id="@item.QuestionId">Edit Topics</a> |
                        <a class="btn btn-link" asp-action="EditQuestionCertificatesIndex" asp-route-id="@item.QuestionId">Edit Certificate</a>
                    </td>*@
                    @*<td>
                        <a class="btn btn-outline-warning" asp-action="Details" asp-route-id="@item.QuestionId">Details</a> 
                    </td>
                    <td>
                        <a class="btn btn-outline-danger" asp-action="Delete" asp-route-id="@item.QuestionId">Delete</a>
                    </td>*@
                </tr>
        }
    </tbody>
</table>
@*<p>
    <a type="button" class="btn btn-primary btn-lg" asp-action="Create">Add New Question</a>
</p*@