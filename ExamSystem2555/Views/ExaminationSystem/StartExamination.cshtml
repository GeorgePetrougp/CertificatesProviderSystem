@model WebApp.DTO_Models.Final.ExaminationQuestionView

<body>

    <h1 style="margin:0 auto;width:50%">ExamSystem</h1>


    <h2>@Html.Raw(Model.Questions[Model.CurrentIndex].Display)</h2>

    <form method="post" asp-action="Examination">

        <input type="hidden" asp-for="CandidateExamId" />
        <input type="hidden" asp-for="ExaminationId" />
        <input type="hidden" asp-for="@Model.CurrentIndex" />
        <input type="hidden" asp-for="Questions[Model.CurrentIndex].QuestionId" />
        <input type="hidden" name="myModel" value="@Newtonsoft.Json.JsonConvert.SerializeObject(Model)" />


        <div>
            @for (int i = 0; i < Model.Questions.Count; i++)
            {
                <button type="submit" value="@i" name="questionIndex">Question@(i+1)</button>
            }
        </div>

        <div class="form-group">
            @foreach (var answer in Model.Questions[Model.CurrentIndex].QuestionPossibleAnswers)
            {
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="SelectedAnswerId" value="@answer.QuestionPossibleAnswerId" @(answer.IsSelected ? "checked" : "" ) />
                    <label class="form-check-label">@Html.Raw(answer.PossibleAnswer)</label>
                </div>
            }
        </div>
        <button type="submit" name="action" value="Next">Next</button>
    </form>

    <a asp-action="Index">Back to List</a>

</body>



@*<script src="~/js/exams.js"></script>*@
@*<script>
    var questions = @Html.Raw(Json.Serialize(Model.Questions));
</script>*@







